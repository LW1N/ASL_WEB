# Welcome to my ASL Web Application
Frontend react native app(built with Expo) is located in /ASL_app

Backend python flask server is located in /python_server
The python server also contains a pre-trained tensorflow model that can predict ASL signs.
